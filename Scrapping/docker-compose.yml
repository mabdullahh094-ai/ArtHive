services:
  art-price-api:
    build: .
    container_name: art-price-api
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
      - ./data:/app/data
    restart: unless-stopped
